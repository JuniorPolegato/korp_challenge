# Local provisioning (run Ansible on the Debian box itself)
[korp]
localhost ansible_connection=local

# Remote example — uncomment and adjust to provision another Debian host
# [korp]
# korp-node-01 ansible_host=192.168.0.50 ansible_user=claudio ansible_connection=ssh

[korp:vars]
ansible_python_interpreter=/usr/bin/python3
